// jquery.radioClass.js
define(['jquery.utils'], function() {
	// 모듈코드

	var plugin = 'radioClass';

	if ( !$.fn[plugin]) {
		$.fn[plugin] = function(options) {
			var $this = this;

			return $.each($this, function(index, el) {
				var 
				
			})
		}
	}

	$.fn[plugin].defaults = 'active';
});